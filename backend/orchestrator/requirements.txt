# Lambda-specific dependencies (minimal set)
# Note: boto3 is already included in Lambda runtime

anthropic>=0.40.0
psycopg2-binary==2.9.9
redis==5.0.1
requests==2.31.0
openai>=1.0.0
mcp>=1.0.0
httpx>=0.27.0

# LangSmith (optional - for evaluation)
langsmith>=0.4.59
langchain-anthropic>=1.3.0
